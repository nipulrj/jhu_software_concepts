models -- the ORM mapping and the Engine
========================================

The ``applicants`` table as SQLAlchemy sees it, and the lazily built
Engine cache that lets one environment variable redirect every query
the application makes.

.. automodule:: models
   :members:
   :member-order: bysource
   :show-inheritance:
