# What Read the Docs installs to build these pages.
#
# Sphinx and its theme, plus the application's own runtime dependencies --
# autodoc imports every module under src/ to read its docstrings, so anything
# those modules import at module level has to be installed here too.
#
# Deliberately NOT the whole of module_4/requirements.txt: that pins
# llama-cpp-python, which is compiled from source and is only needed by the
# optional standardizer subprocess. No module under src/ imports it.

Sphinx==9.1.0
sphinx-rtd-theme==3.1.0

# Imported at module level by the modules autodoc reads.
Flask==3.1.3
SQLAlchemy==2.0.48
psycopg[binary]==3.3.5
beautifulsoup4==4.14.3
