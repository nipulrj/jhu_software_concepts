orm_queries -- the analyses through the ORM
===========================================

The same eleven questions, composed from ``select()``, ``func`` and the
mapped columns.  No ``text()`` and no cursor appears in this module, and
a test tokenizes it to keep that true.  This is what the page reads.

.. automodule:: orm_queries
   :members:
   :member-order: bysource
   :show-inheritance:
