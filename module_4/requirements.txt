# Module 4 - Grad Cafe analytics: the Module 3 application, its Pytest suite and
# its Sphinx documentation.
# Python 3.10+ required (developed and run on CPython 3.13.5, Windows 11).
#
#     python -m pip install -r requirements.txt
#
# Also needs a PostgreSQL server (developed against 18.6) for the `db` and
# `integration` tests; see README.md for the setup and for the scratch database
# those tests expect.

# --- database access (load_data.py, query_data.py) ---
# psycopg 3, the adapter used in lecture. The [binary] extra ships prebuilt
# libpq wheels, so no local PostgreSQL build tooling is needed to install it.
psycopg[binary]==3.3.5

# --- ORM (models.py, orm_queries.py) ---
SQLAlchemy==2.0.48

# --- web application (flask_app.py) ---
Flask==3.1.3

# --- scraping and cleaning (scrape.py, clean.py) ---
beautifulsoup4==4.14.3
soupsieve==2.8.3

# --- tests ---
# pytest-cov is what enforces the 100% line in pytest.ini; it pulls in
# coverage.py itself.
pytest==9.1.1
pytest-cov==7.1.0
coverage==7.16.1

# --- documentation ---
# sphinx.ext.autodoc imports the modules under src/ to read their docstrings, so
# the application's own dependencies above are needed to build the docs too.
Sphinx==9.1.0
sphinx-rtd-theme==3.1.0

# --- local LLM standardizer, carried over (llm_hosting/app.py) ---
# Only needed by the Pull Data button, and only to fill the two LLM columns.
# Without it the pull still runs and still loads the new rows; it just leaves
# llm_generated_program and llm_generated_university empty for them. The test
# suite never invokes it -- the standardizer is injected, and the tests pass a
# double -- so CI does not install it.
#
# llama-cpp-python has no prebuilt wheel for CPython 3.13 on Windows and is
# compiled from source at install time, which needs CMake and the MSVC C++ build
# tools. On a machine without them, install a prebuilt wheel for your Python
# version instead, or run this stage on Python 3.11/3.12.
llama-cpp-python==0.2.90
huggingface_hub==1.31.0

# --- transitive pins, recorded so the environment reconstructs exactly ---
alabaster==1.0.0
babel==2.18.0
blinker==1.9.0
certifi==2026.2.25
charset-normalizer==3.4.6
click==8.5.0
diskcache==5.6.3
docutils==0.22.4
filelock==3.32.6
fsspec==2026.7.0
greenlet==3.3.2
hf-xet==1.6.0
idna==3.11
imagesize==2.0.1
iniconfig==2.3.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
numpy==2.5.2
packaging==26.3
pluggy==1.6.0
Pygments==2.20.0
requests==2.33.1
roman-numerals==4.1.0
snowballstemmer==3.1.1
sphinxcontrib-applehelp==2.0.0
sphinxcontrib-devhelp==2.0.0
sphinxcontrib-htmlhelp==2.1.0
sphinxcontrib-jquery==4.1
sphinxcontrib-jsmath==1.0.1
sphinxcontrib-qthelp==2.0.0
sphinxcontrib-serializinghtml==2.0.0
tqdm==4.70.0
typing_extensions==4.15.0
urllib3==2.6.3
Werkzeug==3.1.8
