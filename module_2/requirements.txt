# Module 2 - Grad Cafe scraper, cleaner, and local LLM standardizer.
# Python 3.10+ required (developed and run on CPython 3.13.5, Windows 11).
#
#     python -m pip install -r requirements.txt
#
# The scraper itself needs only the standard library (urllib, json, re,
# urllib.robotparser) plus BeautifulSoup. Selenium is deliberately NOT used:
# the survey listing is server-rendered HTML that plain urllib retrieves with
# HTTP 200, so a browser adds nothing. See README.md for details.

# --- scraping and cleaning (scrape.py, clean.py) ---
beautifulsoup4==4.14.3
soupsieve==2.8.3

# --- robots.txt evidence capture (capture_robots_evidence.py) ---
# Composes the header onto the headless-Chrome screenshot. Chrome itself is an
# external dependency and is only needed to regenerate screenshot.jpg.
pillow==12.3.0

# --- local LLM standardizer (llm_hosting/app.py) ---
# llama-cpp-python has no prebuilt wheel for CPython 3.13 on Windows and is
# compiled from source at install time; this needs CMake and the MSVC C++ build
# tools. On a machine without them, install a prebuilt wheel for your Python
# version instead, or run this stage on Python 3.11/3.12.
llama-cpp-python==0.2.90
huggingface_hub==1.31.0
Flask==3.1.3

# --- transitive pins, recorded so the environment reconstructs exactly ---
blinker==1.9.0
click==8.5.0
diskcache==5.6.3
filelock==3.32.6
fsspec==2026.7.0
hf-xet==1.6.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
numpy==2.5.2
packaging==26.3
requests==2.33.1
tqdm==4.70.0
typing_extensions==4.15.0
Werkzeug==3.1.8
